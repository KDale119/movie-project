export type actors = {
    id: number,
    dateOfBirth: string,
    firstname: string,
    lastName: string
}
export type directors = {
    id: number,
    dateOfBirth: string,
    firstname: string,
    lastName: string
}
export type movies = {
    id: number,
    movieLength: number,
    movieTitle: string,
    releaseDate: string,
    trailerUrl: string,
}
